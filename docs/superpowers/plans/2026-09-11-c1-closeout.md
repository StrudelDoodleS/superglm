# C1 closeout implementation plan

The user's 2026-09-11 scope correction separates 100-million-row/out-of-core
architecture from the 0.32 release target. This plan replaces the former
multi-packet expansion; its unfinished work is preserved on the local
`deferred/c1-out-of-core` branch at `92f99dda`.

[Acceptance specification](../specs/2026-09-11-c1-closeout.md).

1. [x] Retain the reviewed Gaussian preparation change from `23f5700d`.
   Its original six production/two test hashes were verified: 411 affected
   tests passed and numerical outputs agree exactly. The subsequent
   [Newton integration repair](../../../benchmarks/c1_gaussian_newton_repair.md)
   passes 295 affected tests, including all seven CI failures, and independent
   review. It changes two production files and preserves the numerical formulas.
2. [x] Preserve and remove the out-of-core expansion from this PR. The dedicated
   worktree contains all seven Packet 3 files byte-for-byte, the reviewed row
   store and the original detailed plans. Restore shared compiler/API files to
   their pre-expansion contents. Preserve unrelated QR work.
3. [x] Record the actual current-route 10-million-row fit, including source,
   input hashes, public fit time, peak RSS, iterations, rank, backend and
   timeout receipt. Do not substitute input-generation figures for model fits.
4. [x] Update the roadmap and PR description: C1 core substantially complete;
   100-million-row/out-of-core support separate; 12.5 seconds still unmet.
5. [x] Verify the final solver source and relevant local checks. Production
   source `508dd144` passes all twelve Python 3.12–3.14 shards, their aggregate,
   real-data, development and security CI. Ruff/format, lock/environment and
   strict docs pass. The frozen full-fit control has exactly equal outputs and
   history. Final workflow/documentation changes do not change this solver source.
6. [x] Obtain a normal Fable 5.1/max follow-up review of the changes since the
   completed review. The user clarified that full-PR review was required once;
   that review is already complete. Verify the serving model from the run.
   Run `34621148920` confirms actual Fable/max and no correctness defects.
7. [x] Address required findings and acknowledge the reviews. Earlier blocking
   threads are resolved. The final review's four optional improvements are
   recorded in the Newton repair report, outside this closeout. Codex reports
   no major issues at `508dd144`.
8. [x] Restore the original Opus 5 workflow after the temporary review. CI-only
   PR #384 merged as `20dbd689`; the same restoration passes all 32 workflow
   checks in this branch and matches that approved file exactly.

PR #381's latest required checks remain the integration gate after the final
workflow/documentation commit. Their status is recorded on the PR. Release
assessment, version preparation and publishing follow their separate repository
gates. This plan does not authorize a merge or publication.

No 100-million-row model will be launched in this closeout. The completed
10-million-row process finished in 236.84 s under a 600 s timeout; its public
fit clock is 152.498 s. A dedicated user cgroup was verified to support a
32 GiB memory cap and no swap for a separately scoped future capacity run.
That infrastructure check is not a fitting result.

No version file changes, broad re-audits or third subagent. The existing
numerical contracts and reviewed reuse changes remain the implementation.
